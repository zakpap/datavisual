size(512, 512)
background(1)

import json

with open('word_frequencies.json') as infile:
    freqData = json.load(infile)
print len(freqData)
print freqData[0]

# y = 0

# for word in freqData[:21]:
#     font(12)
#     y = y + 24
#     barwidth = word['freq']/3
#     rect(70,y, barwidth, 20)
#     text(word['word'],10, y + 16)#first wokey of word is loop second is the 
   
